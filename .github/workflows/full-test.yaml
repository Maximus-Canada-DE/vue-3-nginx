name: Lint and test
on: 
  pull_request:
    branches: main
jobs:
  lint-test:  
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/workflows/test-lint-reusable
        with:
          installCommand: npm ci

